# NOTE: This needs to be applied manually after change
# argocd app create --upsert --file ./staging.yaml

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: staging-app-of-apps
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  source:
    repoURL: https://github.com/IFRCGo/go-deploy
    path: applications/argocd/staging
    targetRevision: develop
    directory:
      recurse: true
      jsonnet: {}
      exclude: '{applications/*/internal/*}'
  syncPolicy:
    automated: {}
